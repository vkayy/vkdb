name: Test
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler: [gcc, clang]
    steps:
      - name: Build and run tests
        uses: actions/checkout@v3
        env:
          CXX: ${{ matrix.compiler }}
        run: ./build.sh -t
